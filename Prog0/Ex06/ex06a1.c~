#include <stdio.h>

int main()
{
  int n, sum = 0;
  int i;

  printf("整数を入力してください：");
  scanf("%d", &n);

  for (i = 1, i <= n, i++)
    {
      if (i % 3 == 0)
      {
	sum += i;
      }
    }
  printf("%d以下で3の倍数となる正の整数の和は%dです\n", n, sum);

  return 0;
}
