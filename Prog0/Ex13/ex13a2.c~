#include <stdio.h>

int main(){
  double x, s1, s2;
  int n,nmax;

  printf("何項計算しますか？：");
  scanf("%d", &nmax);

  s1 = 0.0;
  for (n=nmax; n>=1; n--){
    x = (double)n;
    s1 += 1.0/(x*x);
  }
  printf("1/n^2の総和：%.14f\n", s1);
  return 0;
}
