#include <stdio.h>

int main()

{
  int a1, d, n, an;

  printf("Enter a1 d n ->");
  
  scanf("%d%d%d", &a1, &d, &n);

  an = a1 + d * (n - 1);
  
  printf("an = %d\n", an);
  
  printf("Sn = %d\n", (a1 + an) * n / 2);

  return 0;
}
