class SelectionSort{
    public void sort( int[] data ){
        for ( int i = 0; i < N; i++ ) {
	    int min = i;
	    for ( int j = i+1; j < N; j++) {
		if ( data[j] < data[min]) min = j;
	    }
	    int tmp = data[i];
	    data[i] = data[min];
	    data[min] = tmp;
	}
    }
}
